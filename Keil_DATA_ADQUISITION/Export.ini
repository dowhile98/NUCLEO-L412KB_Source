FUNC void save_array() {
int array_length;
int idx;
array_length = 500;

  
  exec("log > y.dat");
  for (idx = 0; idx < array_length; idx++) {
    printf ("%6.4f\n", y[idx]);
  }
  exec("log off");
  
}
